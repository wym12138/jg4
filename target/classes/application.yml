server:
  port: 3213

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dotest_db?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver



mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml # mapper映射文件路径
  type-aliases-package: com.ym.domain   # 配置哪个包下的类有默认的别名
